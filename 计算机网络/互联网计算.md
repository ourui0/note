# 互联网计算

## 计算机网络和因特网

**一、什么是因特网**：

+ 从三个视角定义：

1. **组件视角**：由数以百万计的**端系统（主机）**、**通信链路**（光纤、铜缆、无线电等）和**路由器**构成。
2. **服务视角**：为分布式应用（如Web、电子邮件、流媒体）提供通信服务的**基础设施**，提供可靠或“尽力而为”的数据交付服务。
3. **协议视角**：由控制消息收发格式、顺序和动作的**协议**（如TCP、IP、HTTP、以太网）定义。协议标准主要由IETF（互联网工程任务组）通过RFC制定。

**二、 如何接入因特网**

网络可分为三部分：

- **网络边缘**：运行应用程序的**端系统**（主机），采用客户端/服务器或对等模型。
- **接入网络**：将端系统连接到边缘路由器的物理介质与技术，包括：
  - 住宅接入（如DSL、HFC有线电视网络）。
  - 机构接入（如以太局域网LAN）。
  - 无线接入（如Wi-Fi、蜂窝移动网络）。
- **物理介质**：包括双绞线、同轴电缆、光纤、无线电波等。

**三、 数据如何在网络中传输（网络核心）**

网络核心是互联的路由器网状网络，数据传输主要有两种交换方式：

1. **电路交换**：为通信会话建立专用的端到端连接。性能可预测，但资源利用率可能较低。
2. **分组交换**：将消息分割为**分组**，每个分组独立传输，在路由器中存储转发。资源利用率高，但可能产生排队延迟和丢包，性能不可预测。
   - **统计多路复用** 是分组交换高效的关键，允许多个数据流按需共享链路带宽。
3. **比较**：文档通过表格对比了电路交换、数据报分组交换和虚电路分组交换在传输通路、带宽、路由、时延和扩展性等方面的差异。

**四、 因特网发展简史**

- **1960s-1970s**：分组交换概念诞生（Kleinrock, Baran），ARPANET成为首个分组交换网络。
- **1970s-1980s**：TCP/IP协议架构被提出（Cerf and Kahn），成为互联网互联的基础。DNS、电子邮件等应用出现。
- **1990s**：万维网（WWW）的发明（Tim Berners-Lee）和商业化导致互联网爆炸性增长。
- **2000s至今**：宽带、无线移动网络普及，P2P文件共享、流媒体视频、社交网络、云计算等新型应用不断涌现。

**五、 协议分层与服务模型**

为了解决网络复杂性，采用分层模型。

- **OSI参考模型**：七层结构（物理层、数据链路层、网络层、运输层、会话层、表示层、应用层），是一个理论模型。
- **TCP/IP协议栈**：实际使用的四层模型（网络接口层、网际层、运输层、应用层）。
- **沙漏模型**：IP协议是“沙漏的细腰”，是单一的网络层协议，使上层多样化的应用和下层多样化的网络技术能够互操作，是互联网成功的关键。
- **封装**：数据在发送端从上到下每层添加头部控制信息形成协议数据单元（PDU），在接收端从下到上去除头部。

**六、 网络性能指标**

1. **延迟**：分组从源到目的地的时间，由四部分组成：
   - **传输时延**：将分组所有比特推入链路所需时间（分组长度/链路带宽）。
   - **传播时延**：比特在链路上传播所需时间（链路长度/传播速度）。
   - **排队时延**：分组在路由器缓冲区中等待处理的时间。
   - **处理时延**：路由器检查分组首部和决定其去向所需时间。
2. **丢包**：由于路由器队列溢出而导致分组被丢弃的比例。
3. **吞吐量**：接收方从源接收数据的速率。端到端吞吐量受路径中**最低速率链路**的限制。