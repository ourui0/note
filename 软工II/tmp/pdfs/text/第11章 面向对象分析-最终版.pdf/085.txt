84
2025/5/29
建立用例（多场景）系统顺序图
➢如果在商品销售的系统顺序图中添加下列场景
• 在销售开始时“输入会员编号”，分支点在“开始新
的销售处理”之后，为可选场景（opt片段）；
• “删除一个已输入商品”，分支点在“输入物品项”
及其返回消息之间，为选择场景（alt片段）；
• “取消销售”，建立一个中断场景（break片段）；
• 在“开始新的销售处理”之后、“结账”之前的任意
一点，都允许 “取消销售”，所以将中断场景与
“开始新的销售处理”——“结账”设置为并发场景
（par片段）。
建立交互图