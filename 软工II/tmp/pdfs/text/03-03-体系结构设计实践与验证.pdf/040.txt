依赖倒置原则（DIP）详解
传统依赖
⾼层模块  ── 依赖 ──  低层模块
  (service)          (dao 实现 )
⾼层直接依赖低层具体实现，耦合度⾼。
依赖倒置后
⾼层模块  ── 依赖 ──  抽象接⼝
                      ▲
低层模块  ── 实现 ──┘
⾼层和低层都依赖抽象，依赖⽅向"倒置"。
DIP 两条规则：
1. ⾼层模块不应依赖低层模块，两者都应依赖抽象
2. 抽象不应依赖细节，细节应依赖抽象
40